html
    head
        title= name
    body
        h1 Pozdrowienia od #{name}
        a(href= url) URL
        if(user)
            h1 Hi, #{user.name}
        else
            a(href="/sign_up") Sign Up
